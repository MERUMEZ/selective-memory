1. perplexity, confidence           — как сейчас, без изменений
2. mood.apply_stimulus(...)         — без изменений (побочный эффект)
3. concept extraction + lexical acquisition   — БЕЗ изменений, ВСЕГДА выполняются,
                                        независимо от стадии (усвоение слов идёт
                                        в фоне даже когда бот "физически" не может говорить)

4. ── NEW: ЕДИНЫЙ GATE (выполняется ДО любого поиска/LLM) ──
   a. instincts.is_overloaded()?             → ДА → unified mimicry (Часть 5-старая), СТОП
   b. speech_stage(vocab_size) == 0
      или (в переходной зоне и не повезло с ramp-вероятностью)?
                                              → ДА → unified mimicry, СТОП
   (то есть "аварийный тормоз" по стрессу ПРОВЕРЯЕТСЯ РАНЬШЕ И ВНЕ зависимости
    от того, что случится дальше с памятью — переворачиваем Проблему B из Части 2
    и Проблему №2 из Части 7 одним и тем же фиксом)

5. search() по LTM  — вызывается ТОЛЬКО если прошли шаг 4
   ├── MEMORY_HIT  → build_prompt_context(...) — с ужесточённой инъекцией
   │                  (см. фикс F/E ниже) + лимиты по vocab_size (continuous, фикс G)
   └── MEMORY_MISS → _build_default_prompt_with_stm(...) + те же continuous-лимиты

6. LLM-вызов, fallback на echolalia при сбое — как сейчас